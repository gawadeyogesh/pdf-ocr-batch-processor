# pdf-ocr-batch-processor
A Python-based batch OCR pipeline to convert large multi-language PDFs (200+ pages) into structured text using Tesseract OCR with image preprocessing and memory-safe batch processing
